import {Dispatch} from 'redux';

export const updateUserInfo = () => {
  return async (dispatch: Dispatch) => {
    let balance = '0';
    //if(wallet.address !== '') balance = await getBalance(wallet.address);
    // dispatch(setAccountLoading(false));
    // dispatch(setAccount({
    //   ...wallet, balance: balance
    // }));
  };
};
